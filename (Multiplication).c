#include <stdio.h>

int main()

{
    int A, B, C;
    printf("Enter two numbers to multiply: ");
    scanf("%d %d", &A, &B);
    
    C = A * B;
    printf("The Result is: %d\n", C);
    return 0;
    
}